@PART[ds_boxred]:NEEDS[WildBlueTools]
{
	MODULE
	{
		name = WBIConvertibleStorage
		enableLogging = True
		//Determines if the module allows in-field reconfiguring
		fieldReconfigurable = True
		//Require a confirmation click before changing resources
		confirmResourceSwitch = True
		//Determines whether or not to show the context menu GUI
		showGUI = True
		//Short name of the default module template.
		//This is used when selecting the part in the editor.
		//User will then right-click on the module to change its type.
		defaultTemplate = SpiceMelange
		//name of the template nodes to use
		templateNodes = SPICE_TEMPLATE
		//Some containers don't hold as much resources as the template specifies, while others hold more.
		//Since the resource templates specify base amounts, the capacity factor specifies a multiplier to
		//factor into the resource amounts.
		capacityFactor = 0.14

		//If the part has a KIS container, this is the base and max amount
		baseStorage = .001
		maxStorage = 1000
	}
}

@PART[ds_boxred_flat]:NEEDS[WildBlueTools]
{
	MODULE
	{
		name = WBIConvertibleStorage
		enableLogging = True
		//Determines if the module allows in-field reconfiguring
		fieldReconfigurable = True
		//Require a confirmation click before changing resources
		confirmResourceSwitch = True
		//Determines whether or not to show the context menu GUI
		showGUI = True
		//Short name of the default module template.
		//This is used when selecting the part in the editor.
		//User will then right-click on the module to change its type.
		defaultTemplate = SpiceMelange
		//name of the template nodes to use
		templateNodes = SPICE_TEMPLATE
		//Some containers don't hold as much resources as the template specifies, while others hold more.
		//Since the resource templates specify base amounts, the capacity factor specifies a multiplier to
		//factor into the resource amounts.
		capacityFactor = 0.07

		//If the part has a KIS container, this is the base and max amount
		baseStorage = .001
		maxStorage = 500
	}
}

@PART[ds_boxred_long]:NEEDS[WildBlueTools]
{
	MODULE
	{
		name = WBIConvertibleStorage
		enableLogging = True
		//Determines if the module allows in-field reconfiguring
		fieldReconfigurable = True
		//Require a confirmation click before changing resources
		confirmResourceSwitch = True
		//Determines whether or not to show the context menu GUI
		showGUI = True
		//Short name of the default module template.
		//This is used when selecting the part in the editor.
		//User will then right-click on the module to change its type.
		defaultTemplate = SpiceMelange
		//name of the template nodes to use
		templateNodes = SPICE_TEMPLATE
		//Some containers don't hold as much resources as the template specifies, while others hold more.
		//Since the resource templates specify base amounts, the capacity factor specifies a multiplier to
		//factor into the resource amounts.
		capacityFactor = 0.42

		//If the part has a KIS container, this is the base and max amount
		baseStorage = .001
		maxStorage = 3000
	}
}

SPICE_TEMPLATE:NEEDS[WildBlueTools]
{
	name = SpiceMelange
	author = JadeOfMaar
	shortName = SpiceMelange
	// logoPanel = WildBlueIndustries/000WildBlueTools/Decals/Equipment
	// glowPanel = WildBlueIndustries/000WildBlueTools/Decals/EquipmentGlow
	description = This kit stores the Spice Melange. This omnipotent powder is convertible into many life sustaining substances and even some basic propellants.
	templateTags = stowage

	RESOURCE
	{
		name = SpiceMelange
		amount = 0
		maxAmount = 5600
	}
}

SPICE_TEMPLATE:NEEDS[WildBlueTools]
{
	name = WhiteSpice
	author = JadeOfMaar
	shortName = WhiteSpice
	// logoPanel = WildBlueIndustries/000WildBlueTools/Decals/Equipment
	// glowPanel = WildBlueIndustries/000WildBlueTools/Decals/EquipmentGlow
	description = This kit stores the White Spice. It is a new and even more elusive flavor of the Spice and which best serves as an alternate form of nuclear fuel.
	templateTags = stowage

	RESOURCE
	{
		name = WhiteSpice
		amount = 0
		maxAmount = 5600
	}
}

SPICE_TEMPLATE:NEEDS[WildBlueTools]
{
	name = SpiceGas
	author = JadeOfMaar
	shortName = SpiceGas
	// logoPanel = WildBlueIndustries/000WildBlueTools/Decals/Equipment
	// glowPanel = WildBlueIndustries/000WildBlueTools/Decals/EquipmentGlow
	description = This kit stores the Orange Spice Gas. 

	RESOURCE
	{
		name = SpiceGas
		amount = 0
		maxAmount = 28000000
	}
}
